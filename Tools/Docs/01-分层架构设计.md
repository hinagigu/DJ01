# 01 - 分层架构设计

## 🎯 核心思想

**分层架构**是将系统按职责划分为多个层次，每层只与相邻层交互，降低耦合度。

## 📐 DataAssetManager 的分层结构

```
┌─────────────────────────────────────────────────────┐
│                   表现层 (UI)                        │
│  ui/editors/  ui/widgets/  main.py                  │
│  职责：用户交互、界面渲染、事件处理                    │
├─────────────────────────────────────────────────────┤
│                   业务逻辑层 (Core)                   │
│  core/data_manager.py  core/options_scanner.py      │
│  core/schema_loader.py  core/ue_remote.py           │
│  职责：数据处理、业务规则、外部系统交互                 │
├─────────────────────────────────────────────────────┤
│                   数据层 (Configs)                   │
│  configs/schema/  configs/options/  configs/*.json  │
│  职责：数据存储、配置定义                             │
└─────────────────────────────────────────────────────┘
```

## 🔑 关键要点

### 1. 层间依赖规则

```
UI 层 → 可以调用 → Core 层
Core 层 → 可以调用 → Data 层

✗ Core 层不应该依赖 UI 层
✗ Data 层不应该依赖任何上层
```

**实际代码体现**：

```python
# core/options_scanner.py - 核心层
class OptionsScanner:
    def scan_all(self):
        # 纯业务逻辑，完全不依赖任何 UI 代码
        items = self._scan_directory(path)
        return items

# ui/editors/base.py - UI 层
from core.options_scanner import OptionsScanner  # UI 依赖 Core ✓

class BaseEditor:
    def __init__(self):
        self.scanner = OptionsScanner()  # UI 层调用 Core 层
```

### 2. 为什么这样设计？

| 好处 | 说明 |
|------|------|
| **可测试性** | Core 层可以独立单元测试，无需启动 UI |
| **可替换性** | 换掉 Tkinter 用 Qt，只需重写 UI 层 |
| **团队协作** | 不同人可以并行开发不同层 |
| **复用性** | Core 层可以被命令行工具、Web 服务等复用 |

### 3. 依赖注入

不要在底层"主动获取"依赖，而是让上层"注入"进来：

```python
# ❌ 不好的做法 - 底层主动获取依赖
class DataManager:
    def __init__(self):
        self.scanner = OptionsScanner()  # 硬编码依赖

# ✓ 好的做法 - 依赖注入
class DataManager:
    def __init__(self, scanner: OptionsScanner):
        self.scanner = scanner  # 由外部传入
```

## 📁 目录结构对应

```
DataAssetManager/
├── main.py              # 入口点，组装各层
├── ui/                  # 表现层
│   ├── editors/         #   资产编辑器
│   └── widgets/         #   可复用控件
├── core/                # 业务逻辑层
│   ├── data_manager.py  #   数据管理
│   ├── schema_loader.py #   Schema 加载
│   ├── options_scanner.py # 项目扫描
│   └── ue_remote.py     #   UE 通信
└── configs/             # 数据层
    ├── schema/          #   UI 结构定义
    └── options/         #   扫描结果缓存
```

## 🧪 实践练习

**问题**：如果你想用命令行批量生成资产（不启动 GUI），需要改哪里？

**答案**：只需要新建一个 `cli.py`，直接调用 `core/` 层的类即可：

```python
# cli.py - 命令行入口
from core.data_manager import DataManager
from core.ue_remote import UERemoteExecutor

manager = DataManager()
manager.load_all()

for asset in manager.get_assets("experience"):
    executor.execute_generation(asset)
```

无需改动 `core/` 或 `configs/` 的任何代码！