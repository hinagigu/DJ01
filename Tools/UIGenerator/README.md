# UI Generator - Schema 驱动的 UI 生成工具

## 概述

这是一个 Schema 驱动的 UI 生成工具，用于自动生成 UE Widget 的 C++ 基类和蓝图骨架。

**核心理念**：只需在 `schemas/widgets/` 目录添加 JSON 文件，即可自动生成对应的 UI 代码。

## 快速开始

### 1. 添加 UI

在 `schemas/widgets/` 目录下创建 JSON 文件，例如 `ManaBar.json`：

```json
{
  "name": "DJ01ManaBar",
  "description": "法力条 UI",
  "parent_class": "CommonUserWidget",
  "components": [
    {
      "name": "RootCanvas",
      "type": "CanvasPanel",
      "children": [
        { "name": "ManaBar", "type": "ProgressBar", "bind_percent": "ManaPercent" },
        { "name": "ManaText", "type": "TextBlock", "bind_text": "ManaDisplayText" }
      ]
    }
  ],
  "properties": [
    { "name": "ManaPercent", "type": "float", "default": 1.0 },
    { "name": "ManaDisplayText", "type": "FText" }
  ]
}
```

### 2. 生成代码

```bash
python Tools/UIGenerator/main.py
```

点击 "🚀 全部生成" 按钮，工具会：
- 扫描 `schemas/widgets/` 目录
- 为每个 Schema 生成 C++ 基类

### 3. 编译 & 创建蓝图

1. 在 UE 中编译项目
2. 创建继承 `UDJ01ManaBarBase` 的 Widget Blueprint
3. 添加对应名称的控件（ManaBar, ManaText）
4. 调整位置和样式

## 架构

```
┌─────────────────────────────────────────────────────────────┐
│                   schemas/widgets/                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │HealthBar.json│  │ ManaBar.json │  │ SkillBar.json│ ...  │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────────┬────────────────────────────────┘
                             │ 工具扫描
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   生成 C++ 代码                              │
│  Source/DJ01/UI/Generated/                                  │
│  ├── DJ01HealthBarBase.h / .cpp                            │
│  ├── DJ01ManaBarBase.h / .cpp                              │
│  └── ...                                                   │
└────────────────────────────┬────────────────────────────────┘
                             │ 编译后
                             ▼
┌─────────────────────────────────────────────────────────────┐
│                   创建蓝图子类                               │
│  Content/UI/HUD/                                            │
│  ├── WBP_HealthBar (继承 UDJ01HealthBarBase)               │
│  ├── WBP_ManaBar (继承 UDJ01ManaBarBase)                   │
│  └── ...                                                   │
└─────────────────────────────────────────────────────────────┘
```

## 目录结构

```
Tools/UIGenerator/
├── README.md                 # 说明文档
├── main.py                   # GUI 入口
├── configs/
│   └── widget_types.json     # 支持的控件类型定义
├── schemas/
│   ├── ui_schema_v1.json     # Schema 规范定义
│   ├── widgets/              # ⭐ UI Schema 存放目录
│   │   ├── HealthBar.json
│   │   ├── ManaBar.json
│   │   └── ...
│   └── examples/             # 参考示例
├── core/
│   ├── __init__.py
│   ├── schema_validator.py   # Schema 校验器
│   └── cpp_generator.py      # C++ 代码生成器
└── ue_scripts/
    └── generate_widget_bp.py # UE 内运行的蓝图生成脚本
```