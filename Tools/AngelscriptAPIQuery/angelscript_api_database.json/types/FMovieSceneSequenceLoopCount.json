{
  "name": "FMovieSceneSequenceLoopCount",
  "isStruct": true,
  "doc": "POD struct that represents a number of loops where -1 signifies infinite looping, 0 means no loops, etc\nDefined as a struct rather than an int so a property type customization can be bound to it",
  "properties": {
    "Value": [
      "int",
      "Whether or not to loop playback. If Loop Exactly is chosen, you can specify the number of times to loop"
    ]
  },
  "methods": [
    {
      "name": "opAssign",
      "return": "FMovieSceneSequenceLoopCount&",
      "args": [
        {
          "name": "Other",
          "type": "FMovieSceneSequenceLoopCount"
        }
      ]
    }
  ]
}