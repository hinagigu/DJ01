# UNiagaraDataInterfaceSparseVolumeTexture

**继承自**: `UNiagaraDataInterface`

Data Interface allowing sampling of a sparse volume texture

## 属性

### SparseVolumeTexture
- **类型**: `USparseVolumeTexture`

### SparseVolumeTextureUserParameter
- **类型**: `FNiagaraUserParameterBinding`
- **描述**: When valid the user parameter is used as the texture rather than the one on the data interface

