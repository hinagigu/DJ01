# ASparseVolumeTextureViewer

**继承自**: `AInfo`

A placeable actor that represents a participating media material around a planet, e.g. clouds.

## 属性

### SparseVolumeTextureViewerComponent
- **类型**: `USparseVolumeTextureViewerComponent`

