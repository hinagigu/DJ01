# USingleCameraDirector

**继承自**: `UCameraDirector`

A simple camera director that only ever returns one single camera mode.

## 属性

### CameraMode
- **类型**: `UCameraMode`
- **描述**: The camera mode to run every frame.

