{
  "name": "__ENDIDataChannelSpawnMode",
  "isEnum": true,
  "doc": "Mode controlling the behavior of the Spawn function for Niagara's Data Channel Read Data Interface.",
  "properties": {
    "Override": [
      "ENDIDataChannelSpawnMode",
      "This mode will override any previous spawn counts set or accumulated this tick."
    ],
    "Accumulate": [
      "ENDIDataChannelSpawnMode",
      "This mode will accumulate spawn counts with other spawns."
    ],
    "None": [
      "ENDIDataChannelSpawnMode",
      "Does nothing. This mode is useful for dynamically skipping spawning based on script logic."
    ],
    "Max": [
      "ENDIDataChannelSpawnMode"
    ],
    "ENDIDataChannelSpawnMode_MAX": [
      "ENDIDataChannelSpawnMode"
    ]
  },
  "methods": []
}