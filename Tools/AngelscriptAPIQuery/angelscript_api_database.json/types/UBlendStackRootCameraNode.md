# UBlendStackRootCameraNode

**继承自**: `UCameraNode`

Root camera node for running a camera mode in a blend stack.
This camera node wraps both the camera mode's root node, and the
blend node used to blend it.

