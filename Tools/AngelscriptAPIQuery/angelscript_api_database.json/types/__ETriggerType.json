{
  "name": "__ETriggerType",
  "isEnum": true,
  "doc": "Trigger type determine how the trigger contributes to an action's overall trigger event the behavior of the trigger",
  "properties": {
    "Explicit": [
      "ETriggerType",
      "Input may trigger if any explicit trigger is triggered."
    ],
    "Implicit": [
      "ETriggerType",
      "Input may trigger only if all implicit triggers are triggered."
    ],
    "Blocker": [
      "ETriggerType",
      "Inverted trigger that will block all other triggers when IsBlocking returns true"
    ],
    "ETriggerType_MAX": [
      "ETriggerType"
    ]
  },
  "methods": []
}