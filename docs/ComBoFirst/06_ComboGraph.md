# 第六章：ComboGraph 配置

> **预计耗时**: 2小时  
> **前置要求**: 已完成 [05_AbilityScript.md](./05_AbilityScript.md)  
> **本章目标**: 创建 ComboGraph 资产，配置连招节点和分支

---

## 📋 本章任务清单

- [ ] 理解 ComboGraph 编辑器
- [ ] 创建 Katana 轻攻击连招图
- [ ] 配置 Montage 节点
- [ ] 配置 Edge (连线) 过渡条件
- [ ] 添加分支 (轻/重攻击)
- [ ] 配置效果和 Cue

---

## 6.1 ComboGraph 概述

### 核心概念

```
┌─────────────────────────────────────────────────────────────────┐
│                        ComboGraph                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────┐    Edge    ┌─────────────┐    Edge    ┌─────────┐ │
│  │  Entry  │ ──────────→│ MontageNode │ ──────────→│ Node2   │ │
│  └─────────┘   (输入)   │  (Attack1)  │   (输入)   │(Attack2)│ │
│                         └─────────────┘            └─────────┘ │
│                               │                         │       │
│                               │ Edge (重攻击)           │       │
│                               ▼                         ▼       │
│                         ┌─────────────┐          ┌─────────┐   │
│                         │ Node3       │          │  Node4  │   │
│                         │ (Heavy)     │          │ (Finish)│   │
│                         └─────────────┘          └─────────┘   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 节点类型

| 类型 | 图标 | 用途 |
|------|------|------|
| **Entry** | 🟢 | 入口节点，每个图自动有一个 |
| **Conduit** | 🔷 | 分支入口，根据初始输入选择路径 |
| **Montage** | 🟦 | 播放动画蒙太奇，最常用 |
| **Sequence** | 🟨 | 播放动画序列 (不推荐网络) |

### Edge 属性

| 属性 | 说明 |
|------|------|
| **TransitionInput** | 触发过渡的 InputAction |
| **TransitionBehavior** | 何时执行过渡 |
| **TriggerEvent** | 输入触发类型 |

---

## 6.2 创建 ComboGraph 资产

### 步骤 1: 创建资产

1. 在内容浏览器中导航到:
   ```
   Content/Abilities/ComboGraphs/
   ```
   (如不存在则创建)

2. **右键** → **Miscellaneous** → **Combo Graph**

3. 命名为 `CG_Katana_LightCombo`

### 步骤 2: 打开编辑器

双击资产打开 ComboGraph 编辑器。

界面组成:
- **左侧**: 节点图编辑区
- **右侧 Details**: 选中节点/边的属性
- **工具栏**: 保存、编译等操作

---

## 6.3 添加节点

### 添加第一个攻击节点

1. 在图中 **右键** → **Add Combo Node** → **Montage**
2. 放置在 Entry 节点右侧
3. 选中新节点

### 配置 Montage 节点

在 Details 面板配置:

| 属性 | 值 | 说明 |
|------|-----|------|
| **Node Title** | `Attack_1` | 显示名称 |
| **Montage** | `AM_Katana_ComboA_1` | 攻击蒙太奇 |
| **Montage Play Rate** | 1.0 | 播放速率 |
| **Montage Start Section** | (空) | 从默认 Section 开始 |

### 添加更多节点

重复上述步骤，添加:
- `Attack_2` → `AM_Katana_ComboA_2`
- `Attack_3` → `AM_Katana_ComboA_3`
- `Attack_4` → `AM_Katana_ComboA_4`

---

## 6.4 连接节点 (Edge)

### 创建连线

1. 从 **Entry** 节点边缘拖出
2. 连接到 **Attack_1** 节点
3. 重复: Attack_1 → Attack_2 → Attack_3 → Attack_4

### 配置 Edge - Entry → Attack_1

选中 Entry 到 Attack_1 的连线:

| 属性 | 值 | 说明 |
|------|-----|------|
| **Transition Input** | `IA_LightAttack` | 轻攻击输入 |
| **Trigger Event** | `Started` | 按下时触发 |

> 💡 **注意**: 如果使用 Gameplay Event 触发技能，Entry 的连线可以不设置输入，技能激活时会自动进入第一个节点。

### 配置 Edge - Attack_1 → Attack_2

选中连线:

| 属性 | 值 | 说明 |
|------|-----|------|
| **Transition Input** | `IA_LightAttack` | 轻攻击输入 |
| **Transition Behavior** | `OnComboWindowEnd` | 窗口结束后过渡 |
| **Trigger Event** | `Started` | 按下时触发 |

### Transition Behavior 选项

| 值 | 说明 | 使用场景 |
|----|------|---------|
| `Immediately` | 立即过渡 | 快速连击 |
| `OnComboWindowEnd` | 窗口结束后 | 标准连招 |
| `OnAnimNotifyClass` | 特定 Notify 触发时 | 精确控制 |
| `OnAnimNotifyName` | 指定名称的 Notify | 精确控制 |

---

## 6.5 完整的4段连招配置

### 节点布局

```
[Entry] ──→ [Attack_1] ──→ [Attack_2] ──→ [Attack_3] ──→ [Attack_4]
             │                                              │
             └── ComboWindow ──→ 可接受下一段输入 ───────────┘
```

### 各 Edge 配置

| Edge | Transition Input | Behavior | Trigger |
|------|-----------------|----------|---------|
| Entry → Attack_1 | `IA_LightAttack` | - | `Started` |
| Attack_1 → Attack_2 | `IA_LightAttack` | `OnComboWindowEnd` | `Started` |
| Attack_2 → Attack_3 | `IA_LightAttack` | `OnComboWindowEnd` | `Started` |
| Attack_3 → Attack_4 | `IA_LightAttack` | `OnComboWindowEnd` | `Started` |

### 各 Node 配置

| Node | Montage | Play Rate |
|------|---------|-----------|
| Attack_1 | `AM_Katana_ComboA_1` | 1.0 |
| Attack_2 | `AM_Katana_ComboA_2` | 1.0 |
| Attack_3 | `AM_Katana_ComboA_3` | 1.0 |
| Attack_4 | `AM_Katana_ComboA_4` | 1.0 |

---

## 6.6 添加分支 (轻/重攻击)

### 使用 Conduit 节点

如果想在连招开始时就区分轻/重:

```
              ┌──→ [Light_1] → [Light_2] → ...
[Entry] ──→ [Conduit]
              └──→ [Heavy_1] → [Heavy_2] → ...
```

### 配置 Conduit

1. **右键** → **Add Combo Node** → **Conduit**
2. 放置在 Entry 和攻击节点之间
3. 从 Conduit 拖出多条连线

### Conduit Edge 配置

| Edge | Transition Input |
|------|-----------------|
| Conduit → Light_1 | `IA_LightAttack` |
| Conduit → Heavy_1 | `IA_HeavyAttack` |

### 运行时初始输入

在技能中设置 `InitialInputAction`:
- 如果按的是轻攻击键 → Conduit 走 Light 路径
- 如果按的是重攻击键 → Conduit 走 Heavy 路径

---

## 6.7 分支切换 (连招中变招)

### 从连招中分支到重攻击

```
[Attack_1] ──→ [Attack_2] ──→ [Attack_3]
     │              │              │
     └──→ [Heavy_1] ←──┘          │
                     └──→ [Heavy_2]←┘
```

### 配置多条 Edge

从同一个节点拖出多条连线:

**Attack_1 的两条 Edge**:
| Edge | 目标 | Input | Behavior |
|------|------|-------|----------|
| 1 | Attack_2 | `IA_LightAttack` | `OnComboWindowEnd` |
| 2 | Heavy_1 | `IA_HeavyAttack` | `OnComboWindowEnd` |

### 输入优先级

当多条 Edge 的条件都满足时，优先级由编辑器中的顺序决定。可以通过重新连线调整顺序。

---

## 6.8 配置效果 (GameplayEffect)

### Effects Container Map

在 Montage 节点的 Details 中找到 `Effects Container Map`:

```
Effects Container Map:
├── Key: Event.Damage.Hit
│   └── Value (FComboGraphGameplayEffectContainer):
│       ├── TargetGameplayEffectClasses:
│       │   └── [0] GE_Damage_Light
│       ├── bUseSetByCallerMagnitude: true
│       └── SetByCallerMagnitude: 25.0
│       └── SetByCallerDataTag: Data.Damage
│
└── Key: Event.Buff.ApplyOnHit
    └── TargetGameplayEffectClasses:
        └── [0] GE_ApplySlow
```

### 触发效果的事件

效果通过 AnimNotify 发送的 GameplayEvent 触发:

1. 在蒙太奇中添加 `Send Gameplay Event` Notify
2. 设置 Event Tag: `Event.Damage.Hit`
3. ComboGraph 收到事件后自动应用配置的 Effect

---

## 6.9 配置 Cue (特效/音效)

### Cues Container Map

```
Cues Container Map:
├── Key: Event.Damage.Hit
│   └── CueTags:
│       └── [0] GameplayCue.Combat.Hit.Light
│
└── Key: Event.Combo.Finisher
    └── CueTags:
        └── [0] GameplayCue.Combat.Finisher
```

### GameplayCue 资产

确保项目中有对应的 GameplayCue Handler:
- `GC_Combat_Hit_Light` - 轻击命中特效
- `GC_Combat_Finisher` - 终结技特效

---

## 6.10 高级配置

### 节点消耗 (Cost)

为单个节点配置资源消耗:

| 属性 | 说明 |
|------|------|
| **Cost Gameplay Effect** | 消耗效果 (扣蓝等) |

### Root Motion 缩放

| 属性 | 说明 |
|------|------|
| **Root Motion Scale** | FVector, 缩放根运动位移 |

例如: `(1.0, 1.0, 0.0)` 保留水平运动，忽略垂直运动

### 节点蓝图事件

可以在节点蓝图中处理:

| 事件 | 时机 |
|------|------|
| `CanActivateNode` | 判断节点是否可激活 |
| `OnActivated` | 节点激活时 |
| `OnDeactivated` | 节点停用时 |
| `OnMontagePlay` | 蒙太奇开始播放 |
| `OnEventReceived` | 收到事件 |

---

## 6.11 调试 ComboGraph

### 启用调试器

1. 运行游戏
2. 打开 ComboGraph 编辑器
3. 选择要调试的 Actor
4. 观察当前节点高亮

### 控制台日志

```
Log LogComboGraph Verbose
```

### 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| 连招不继续 | ComboWindow 未添加 | 检查蒙太奇的 Notify |
| 输入不响应 | Edge 未配置输入 | 检查 TransitionInput |
| 动画跳过 | Behavior 设为 Immediately | 改为 OnComboWindowEnd |
| 分支不生效 | Conduit 配置错误 | 检查各 Edge 的 Input |

---

## 6.12 完整 ComboGraph 示例

### CG_Katana_LightCombo 布局

```
                                    ┌─── IA_HeavyAttack ───→ [Heavy_Finisher]
                                    │
[Entry] ──→ [Attack_1] ──→ [Attack_2] ──→ [Attack_3] ──→ [Attack_4]
                │                                              
                └─── IA_HeavyAttack ───→ [Heavy_2]            
```

### 节点配置总结

| 节点 | Montage | 效果 |
|------|---------|------|
| Attack_1 | AM_Katana_ComboA_1 | Event.Damage.Hit → 25伤害 |
| Attack_2 | AM_Katana_ComboA_2 | Event.Damage.Hit → 30伤害 |
| Attack_3 | AM_Katana_ComboA_3 | Event.Damage.Hit → 35伤害 |
| Attack_4 | AM_Katana_ComboA_4 | Event.Damage.Hit → 50伤害, Finisher Cue |
| Heavy_2 | AM_Katana_Heavy_1 | Event.Damage.Hit → 60伤害 |
| Heavy_Finisher | AM_Katana_Heavy_2 | Event.Damage.Hit → 80伤害 |

---

## 6.13 验证清单

### 资产验证

- [ ] ComboGraph 资产已创建
- [ ] 所有节点引用了正确的 Montage
- [ ] 所有 Edge 配置了输入

### 蒙太奇验证

- [ ] 每个蒙太奇有 ComboWindow Notify (除最后一段)
- [ ] 有发送 GameplayEvent 的 Notify (用于伤害)

### 技能集成验证

- [ ] 技能的 ComboGraph 属性指向此资产
- [ ] 技能可以正常激活

### 运行时验证

- [ ] 第一段攻击正常播放
- [ ] 在窗口内按键可继续下一段
- [ ] 分支输入正确触发对应路径
- [ ] 最后一段结束后技能正常结束

---

## 6.14 下一步

完成本章后，你应该有:

✅ `CG_Katana_LightCombo` 资产  
✅ 完整的4段连招配置  
✅ 轻/重攻击分支  
✅ 效果和 Cue 配置

最后一章我们将进行整合测试和问题排查。

---

👉 **[进入第七章：整合测试](./07_Integration.md)**